name: Docker_Image_CI

env:
  APP : "api-${{ github.sha }}" 

on:
  push:
    branches: 
      - main


jobs:
  create:
    runs-on: self-hosted


    steps:
    - name: create jobs world
      run : |
        ls
        echo "App-name: ${{ env.APP }}"
        echo ${{github.action_path}}
        echo ${{github.run_number}}
        echo ${{github.workspace}}
  build:
    runs-on: ubuntu-latest
    needs: [create]

    steps:
    - name: Print Hello world
      run : echo hello world